<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user01.fuber">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FirstActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PostScreen"
            android:label="@string/title_activity_post_screen"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".GetScreen"
            android:label="@string/title_activity_get_screen"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ViewListContents"
            android:label="List Item"></activity>
        <activity android:name=".DisplayActivity"></activity>
    </application>

</manifest>